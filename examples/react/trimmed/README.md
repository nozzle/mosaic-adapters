# `example-react-trimmed`

React example app shell for the Mosaic data-client packages. The athletes
dashboard (table + visualization on the new clients) is rebuilt here in Phase 4
of the [data-first re-architecture](https://github.com/nozzle/mosaic-adapters/issues/131).

## Run it

```bash
pnpm --filter example-react-trimmed dev
```
