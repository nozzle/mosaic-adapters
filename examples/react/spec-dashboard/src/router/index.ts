/**
 * The router's import surface: app code imports from `@/router` and gets the
 * React bindings (hooks + types). The framework-agnostic core in `./core` is an
 * implementation detail and is deliberately not exported here.
 */
export * from './react';
